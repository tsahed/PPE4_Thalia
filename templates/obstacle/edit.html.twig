{% extends 'base.html.twig' %}

{% block title %}Réservation{% endblock %}

{% block body %}
    <h1>Edit Obstacle</h1>

    {{ include('obstacle/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('obstacle_index') }}">back to list</a>

    {{ include('obstacle/_delete_form.html.twig') }}
{% endblock %}
