{% extends 'base.html.twig' %}

{% block title %}PositionObstacle{% endblock %}

{% block body %}
    <h1>PositionObstacle</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ position_obstacle.id }}</td>
            </tr>
            <tr>
                <th>Position</th>
                <td>{{ position_obstacle.position }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('position_obstacle_index') }}">back to list</a>

    <a href="{{ path('position_obstacle_edit', {'id': position_obstacle.id}) }}">edit</a>

    {{ include('position_obstacle/_delete_form.html.twig') }}
{% endblock %}
