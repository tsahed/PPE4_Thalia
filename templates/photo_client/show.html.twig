{% extends 'base.html.twig' %}

{% block title %}PhotoClient{% endblock %}

{% block body %}
    <h1>PhotoClient</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ photo_client.id }}</td>
            </tr>
            <tr>
                <th>Photo</th>
                <td>{{ photo_client.photo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('photo_client_index') }}">back to list</a>

    <a href="{{ path('photo_client_edit', {'id': photo_client.id}) }}">edit</a>

    {{ include('photo_client/_delete_form.html.twig') }}
{% endblock %}
